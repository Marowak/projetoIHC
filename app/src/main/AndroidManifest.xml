<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rodrigo.minhasreceitas">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConfiguracoesActivity"
            android:label="Configurações"/>
        <activity
            android:name=".NovaCategoriaActivity"
            android:label="Nova Categoria"></activity>
        <activity
            android:name=".CadastroActivity"
            android:label="Cadastro" />
        <activity
            android:name=".CategoriaListActivity"
            android:label="Categorias"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ReceitaListActivity"
            android:label="Sobremesa"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ReceitaVaziaActivity"
            android:label="Mexicana"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>